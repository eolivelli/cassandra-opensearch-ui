<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.8</version>
        <relativePath/>
    </parent>

    <groupId>com.dbui</groupId>
    <artifactId>h2o-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>H2O Parent</name>
    <description>Parent aggregator for the Cassandra + OpenSearch demos: the DB UI console and the H2O Store</description>
    <url>https://github.com/eolivelli/cassandra-opensearch-ui</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <modules>
        <module>dbui</module>
        <module>store</module>
        <module>store-cli</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Shared library versions -->
        <cassandra-driver.version>4.18.1</cassandra-driver.version>
        <testcontainers.version>1.20.4</testcontainers.version>
        <opensearch-testcontainers.version>3.0.1</opensearch-testcontainers.version>

        <!-- Build plugin versions -->
        <spotless.version>2.44.5</spotless.version>
        <frontend-plugin.version>1.15.1</frontend-plugin.version>

        <!-- Front-end toolchain (used by the store module) -->
        <node.version>v20.18.0</node.version>
        <npm.version>10.8.2</npm.version>

        <!-- Location of the shared Spotless configuration, resolved from the repo root so it
             works identically for every module. -->
        <spotless.config.dir>${maven.multiModuleProjectDirectory}/spotless</spotless.config.dir>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Cassandra: DataStax Java driver -->
            <dependency>
                <groupId>org.apache.cassandra</groupId>
                <artifactId>java-driver-core</artifactId>
                <version>${cassandra-driver.version}</version>
            </dependency>

            <dependency>
                <groupId>org.opensearch</groupId>
                <artifactId>opensearch-testcontainers</artifactId>
                <version>${opensearch-testcontainers.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.github.eirslett</groupId>
                    <artifactId>frontend-maven-plugin</artifactId>
                    <version>${frontend-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!-- Spotless is configured once here and applied to every module. Paths are
                 relative to each module's basedir; the shared license header and formatter
                 profile live at the repo root (see ${spotless.config.dir}). -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <!-- The Eclipse JDT formatter is used because, on JDK 25,
                             google-/palantir-java-format cannot access the changed
                             javac internals (NoSuchMethodError). -->
                        <eclipse>
                            <file>${spotless.config.dir}/eclipse-formatter.prefs</file>
                        </eclipse>
                        <removeUnusedImports>
                            <engine>cleanthat-javaparser-unnecessaryimport</engine>
                        </removeUnusedImports>
                        <importOrder/>
                        <licenseHeader>
                            <file>${spotless.config.dir}/license-header.txt</file>
                        </licenseHeader>
                    </java>
                    <!-- Front-end and config files: enforce whitespace hygiene. The React
                         sources under src/main/frontend are formatted by their own tooling
                         and excluded here. -->
                    <formats>
                        <format>
                            <includes>
                                <include>src/main/resources/static/*.js</include>
                                <include>src/main/resources/static/*.css</include>
                                <include>src/main/resources/static/*.html</include>
                                <include>src/main/resources/*.yml</include>
                                <include>*.md</include>
                            </includes>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                        </format>
                    </formats>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
